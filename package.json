{
  "name": "coding-test-mini-clinical-note-coding-assistant",
  "version": "1.0.0",
  "description": "coding test for an AI company",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -k -n server,web -c auto \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
